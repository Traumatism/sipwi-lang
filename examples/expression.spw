main <- proc [] do
    a <- 1
    b <- 100

    x <- (
        y <- ([1; 1] |> sum)
        [a; b]
            |> irange
            |> sum
            |> gauss_sum
    )

    x |> puts
    nl |> puts
end

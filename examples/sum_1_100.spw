sum_a_to_b <- fnc [a; b] do
    value <- {[a; b] |> irange |> sum}
    [value; nl] |> puts
end

sum_1_to_100_included <- fnc [] do
    [1; 101] |> sum_a_to_b
end

sum_1_to_100 <- fnc [] do
    [1; 100] |> sum_a_to_b
end

main <- fnc [] do
    [] |> sum_1_to_100
    [] |> sum_1_to_100_included
end

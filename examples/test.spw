main <- fnc [] do
    "hello world" |> puts

end

`
f <- fnc [a; b; c] do
    [
        "this is arg a: "; a; nl;
        "this is arg b: "; b; nl;
        "this is arg c: "; c; nl;
    ] |> puts
end

dice <- fnc [] do
    [1; 6] |> randint |> puts
    [nl] |> puts
end

main <- fnc [] do
    [1337; "kawai"; -123] |> f

    [nl] |> puts

    [] |> dice
    [] |> dice
    [] |> dice
end
`